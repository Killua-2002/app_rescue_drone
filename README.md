git init
git add .
git commit -m "ghi note vo"
git remote add origin <link github project>
git push #đẩy data lên
git push -u origin main # đẩy data vào nhánh main
git pull # lấy data xuống
git clone <link> #lấy 1 project clone